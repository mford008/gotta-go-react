import {React, Component} from 'react';

class NavbarBurger extends Component {
  state = {
    isActive:
  }
  render () {
    return (
      <NavbarBurger>
        {( children )}
      </NavbarBurger>
    );
  }
}
export default NavbarBurger;
